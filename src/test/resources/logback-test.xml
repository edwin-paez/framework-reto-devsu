<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Consola: INFO para el runner, DEBUG para las llamadas HTTP de Karate -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Archivo: log completo de requests/responses HTTP -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>build/karate.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Karate HTTP logging al archivo -->
    <logger name="com.intuit.karate" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>
